import styled from 'styled-components';

export const Button = styled.button`
  width: max-content;
  height: max-content;
  align-self: center;
  background: transparent;
  cursor: pointer;
  border: none;
`;
